import { FadeOnHover } from './fade-on-hover';

describe('FadeOnHover', () => {
  it('should create an instance', () => {
    const directive = new FadeOnHover();
    expect(directive).toBeTruthy();
  });
});
