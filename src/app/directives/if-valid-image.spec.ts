import { IfValidImage } from './if-valid-image';

describe('IfValidImage', () => {
  it('should create an instance', () => {
    const directive = new IfValidImage();
    expect(directive).toBeTruthy();
  });
});
