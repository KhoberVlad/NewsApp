import { ShowIfNew } from './show-if-new';

describe('ShowIfNew', () => {
  it('should create an instance', () => {
    const directive = new ShowIfNew();
    expect(directive).toBeTruthy();
  });
});
